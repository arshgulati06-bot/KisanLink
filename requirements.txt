flask>=3.0.0
flask-cors>=4.0.0
pandas>=2.0.0
numpy>=1.24.0
torch>=2.0.0
chronos-forecasting>=1.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
pytest>=7.0.0
# ml/config.py loads .env at import time
python-dotenv>=1.0.0
# reading the 2023/2024/2025 mandi workbooks
openpyxl>=3.1.0
# ml/quality_inference.py: MobileNetV3-Large checkpoints + image decoding.
# Install the CPU wheels from https://download.pytorch.org/whl/cpu so the
# torch/torchvision pair matches — a mismatched pair fails at import with
# "torchvision::nms does not exist".
torchvision>=0.15.0
Pillow>=10.0.0
