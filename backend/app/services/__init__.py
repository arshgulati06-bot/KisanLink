"""
Business logic.

Controllers translate HTTP into arguments, services decide what happens, and
repositories talk to the database. Services are the only layer that enforces
rules like "a buyer cannot offer on a sold lot".
"""
