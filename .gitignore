# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg

# ── Virtual Environments ──
.venv/
venv/
ENV/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ──
Thumbs.db
.DS_Store
desktop.ini

# ── HuggingFace / Model Cache ──
.cache/

# ── ML Outputs (generated at runtime) ──
ml/outputs/*.png

# ── ML Dataset (52 MB — use Git LFS or download separately) ──
# ── ML Dataset (large CSVs — not in git) ──
ml/data/*.csv
ml/data/_cache/
ml/data/ingested/ingest.log
ml/data/ingested/records.csv
*.bak

# ── Node (if any) ──
node_modules/

# ── Byte-compiled ──
*.pyc
